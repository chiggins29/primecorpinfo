<% @blogs.each do |blog| %>
	<%= blog.catagory %><br>
	<%= link_to blog.title, blog_path(blog) %><br>
	<%= blog.description %><br>
	<%= blog.body %><br>
	<%= link_to blog.status, toggle_status_blog_path(blog) if logged_in?(:admin) %><br><hr>
<% end %>