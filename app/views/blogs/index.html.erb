<% @blogs.each do |blog| %>
	<%= link_to blog.status, toggle_status_blog_path(blog) %><br>
	<%= link_to blog.title, blog_path(blog.id) %><br>
	<%= blog.description %><br>
	<%= blog.body %><br><hr>
<% end %>