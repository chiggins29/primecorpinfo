
	<%= @review.full_name %><br>
	<%= @review.service_purchased %><br>
	<%= @review.body %><br>
	
	<%= link_to "Delete", @review, method: :delete, data: {confirm: "Are you sure you want to delete this review?"} if logged_in?(:admin) %><br><hr>
